<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.mingge.mapper.MenuMapper">
    <insert id="addRoleMenu">
        insert into sys_role_menu(role_id,menu_id) VALUES
            <foreach collection="menuIds" item="menuId" separator=",">
                (#{id},#{menuId})
            </foreach>
    </insert>
    <delete id="deleteRoleMenu">
        delete from sys_role_menu where role_id = #{id}
    </delete>

    <select id="selectPermsByUserId" resultType="java.lang.String">
        SELECT
            DISTINCT m.perms
        FROM
            `sys_user_role` ur
                LEFT JOIN `sys_role_menu` rm ON ur.`role_id` = rm.`role_id`
                LEFT JOIN `sys_menu` m ON m.`id` = rm.`menu_id`
        WHERE
            ur.`user_id` = #{userId} AND
            m.`menu_type` IN (#{c},#{f}) AND
            m.`status` = 0 AND
            m.`del_flag` = 0
    </select>
    <select id="selectAllRouterMenu" resultType="com.mingge.domain.entity.Menu">
        SELECT
            DISTINCT m.id, m.parent_id, m.menu_name, m.path, m.component, m.visible, m.status, IFNULL(m.perms,'') AS perms, m.is_frame,  m.menu_type, m.icon, m.order_num, m.create_time
        FROM
            `sys_menu` m
        WHERE
            m.`menu_type` IN ('C','M') AND
            m.`status` = 0 AND
            m.`del_flag` = 0
        ORDER BY
            m.parent_id,m.order_num
    </select>
    <select id="selectRouterMenuTreeByUserId" resultType="com.mingge.domain.entity.Menu">
        SELECT
            DISTINCT m.id, m.parent_id, m.menu_name, m.path, m.component, m.visible, m.status, IFNULL(m.perms,'') AS perms, m.is_frame,  m.menu_type, m.icon, m.order_num, m.create_time
        FROM
            `sys_user_role` ur
                LEFT JOIN `sys_role_menu` rm ON ur.`role_id` = rm.`role_id`
                LEFT JOIN `sys_menu` m ON m.`id` = rm.`menu_id`
        WHERE
            ur.`user_id` = #{userId} AND
            m.`menu_type` IN ('C','M') AND
            m.`status` = 0 AND
            m.`del_flag` = 0
        ORDER BY
            m.parent_id,m.order_num
    </select>
    <select id="selectAllRouterMenuTreeselectVo" resultType="com.mingge.domain.vo.MenuTreeselectVo">
        SELECT
            DISTINCT m.id, m.parent_id, m.menu_name label
        FROM
            `sys_menu` m
        WHERE
            m.`status` = 0 AND
            m.`del_flag` = 0
        ORDER BY
            m.parent_id
    </select>
    <select id="roleMenuTreeselectByid" resultType="com.mingge.domain.vo.MenuTreeselectVo">
        select DISTINCT sm.id,sm.menu_name label,sm.parent_id
        from sys_role sr
                 LEFT JOIN sys_role_menu srm ON sr.id = srm.role_id
                 LEFT JOIN sys_menu sm ON srm.menu_id = sm.id
        WHERE sr.id = #{id} AND
            sm.`status` = 0 AND
            sm.`del_flag` = 0
    </select>


</mapper>